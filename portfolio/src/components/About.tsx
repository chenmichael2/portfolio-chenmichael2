import Image from "next/image";

export default function Hero() {
  return (
    <section id="about" className="flex flex-col items-center justify-center w-full h-screen bg-transparent animate__animated animate__fadeIn -translate-y-30 z-2">
      <div> This is the About</div>
    </section>
  );
}